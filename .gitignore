# 系统与编辑器
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# 依赖与构建产物
node_modules/
dist/

# 本地私有数据（如果以后把用户数据导出到文件，别提交上来）
*.local.json
backup/
*.backup.json

# 只放在本机、方便复制粘贴到云开发控制台的副本（和 cloud/ 里的正式文件内容一致）
新云函数-复制这个文件的内容.js
升级SQL-在控制台执行这个.sql

# 提交信息用的临时文件
_msg*.txt

# 上传到静态托管的网页文件（由 index.html + assets 打包而来）
上传到腾讯云-网页文件/
cocktail-static.zip

# PPT 制作的中间产物
.deck-build/
.codex-finalizer/
